# Arcaevo — Build State

This file is the single source of truth for the autonomous overnight build loop.
Every loop iteration: read this file first, do the next unchecked work, update this file, commit.

## Architecture decisions (locked)

- **Monorepo layout**: `apps/web` (Next.js 15 App Router + TS + Tailwind, standalone npm project), `apps/ios` (Swift/SwiftUI iOS + watchOS via XcodeGen `project.yml`), `infra/cdk` (AWS CDK v2, TypeScript, standalone npm project), `design_handoff/` (versioned copy of the design package), `docs/`.
- **No npm workspaces** — each JS project is self-contained to avoid hoisting issues.
- **Styling**: Tailwind v4 with the handoff design tokens mapped in `globals.css` `@theme`.
- **Fonts**: `next/font/google` — Instrument Serif, Hanken Grotesk, Geist Mono.
- **Data**: MongoDB (local via docker-compose; Atlas in prod). Driver: official `mongodb` package via `apps/web/src/lib/db.ts`. No Mongoose.
- **Auth (admin)**: v1 = signed session cookie + single admin password from env (`ADMIN_PASSWORD`). Documented as mock/placeholder in MOCKED_APIS.md.
- **Payments**: Stripe — MOCKED (no keys). Blood testing: LetsGetChecked — MOCKED. See docs/MOCKED_APIS.md.
- **Analytics**: PostHog EU — stubbed off by default (no US-hosted scripts, per handoff).
- **Region**: eu-west-1 everywhere in CDK.
- **Hosting (user-confirmed 2026-07-02)**: Vercel (EU region, fra1/dub1) is the primary host for apps/web — fastest to market. The Dockerfile + docker-compose stack is kept working at all times so we can move to AWS (ECS/Fargate via CDK) later without a rewrite. CDK covers the AWS-side footprint only (secrets, exports bucket); no duplicate web hosting infra unless we migrate.

## Ground rules for agents

- Do NOT run `npm install` or edit `package.json` in apps/web — deps are preinstalled (next, react, tailwind v4, mongodb, zod). If you need another dep, record it under "Wanted deps" below and code around it.
- Pixel-fidelity to `design_handoff/designs/*.dc.html`; copy text verbatim. No invented copy.
- v1 integrations: Apple Watch + Apple Health only; WHOOP/Oura/Garmin only ever "on the roadmap".
- Wellness language, never diagnosis. Keep all disclaimers from designs.
- Prices verbatim: Fusion €119/yr · Essential €329/yr (MOST POPULAR) · Performance €399/yr; annual only; quarterly upgrade +€130/yr; add-ons €99 full panel / €69 recheck / €199 venous draw.

## Status

### Phase 0 — Repo foundation
- [x] Monorepo dirs, design handoff copied to `design_handoff/`
- [x] Next.js scaffolded at `apps/web`, deps installed (incl. mongodb, zod)
- [x] docker-compose.yml (mongo + mongo-express + web)
- [x] docs/MOCKED_APIS.md
- [x] Initial commit

### Phase 1 — Web foundation (apps/web)
- [x] Design tokens in globals.css, fonts in layout, base metadata
- [x] SiteNav + SiteFooter components
- [x] Home `/` (pixel-fidelity)
- [x] Pricing `/pricing`

### Phase 2 — Content extraction (apps/web/src/content)
- [x] Versus/compare data → `src/content/compare.ts`
- [x] Blog articles → `src/content/articles.ts`
- [x] Legal docs → `src/content/legal.ts`
- [x] Help FAQ groups → `src/content/help.ts`

### Phase 3 — Remaining marketing pages
- [x] /how-it-works, /science, /app, /about, /careers, /contact
- [x] /compare + /compare/[slug], /blog + /blog/[slug], /legal/[doc], /help

### Phase 4 — SEO/AEO
- [x] Site-wide SEO infra: src/lib/seo.ts (SITE_URL, canonicalUrl, jsonLd, Organization/Product schema), Organization+Product JSON-LD on Home & Pricing, canonicals for / and /pricing, sitemap.ts (30 URLs), robots.ts (disallow /admin,/api), default opengraph-image.tsx + twitter-image.tsx
- [x] Per-route metadata + FAQPage/Article/BreadcrumbList JSON-LD (shipped by the route-building agents: FAQPage+Breadcrumb on versus, Article+FAQPage on blog posts)

### Phase 5 — API + data layer (apps/web)
- [x] Mongo connection lib + typed models (User, Membership, TestOrder, BiomarkerReading, BiomarkerRule, WearableSignal)
- [x] Seed script (`npm run seed`)
- [x] REST API routes under /api/v1 (members, orders, results, insights, sync) consumed by iOS app + admin
- [x] Mock LetsGetChecked adapter + mock Stripe adapter behind interfaces
- [x] Admin auth (env password session)

### Phase 6 — Admin dashboard
- [x] /admin auth-gated skeleton per Admin.dc.html (dashboard, members, results, support tabs) — login + HMAC-cookie gate, (panel) route group with admin chrome/sidebar, 4 tab pages, review sign-off action via POST /api/v1/admin/results/[id]/review (verified live: queue 30 → 29), graceful Mongo-down/empty-DB notices with `npm run seed` hint. Agent stalled during a final edge-case check; core flows verified, build green with all /admin routes.

### Phase 7 — iOS + watchOS (apps/ios)
- [x] XcodeGen project.yml (iOS app + watchOS app targets)
- [x] SwiftUI iOS app: onboarding, dashboard (baseline/insights), results, orders, settings
- [x] HealthKit integration layer (real reads where possible; mock data source fallback)
- [x] watchOS companion: today ring + latest insight
- [x] API client pointing at web /api/v1
- [x] Builds with xcodebuild — unblocked 2026-07-03: -runFirstLaunch installed missing system content without sudo; BUILD SUCCEEDED locally.

### Phase 8 — Infra (infra/cdk)
- [x] CDK app: eu-west-1; stacks documented (Atlas is external — document connection via secrets)
- [x] `cdk synth` passes

### Phase 9 — Tests + verification (USER REQUIREMENT: e2e-testable when done)
- [x] `npm run build` passes in apps/web
- [x] Unit tests (vitest) for lib logic (RCV verdicts, refund rules, vendor mocks)
- [x] Playwright e2e suite: 24 tests / 7 specs, all green (routes+h1/title, link crawl, pricing verbatim, help accordion, admin login/tabs/sign-off, API smoke incl. mock-LGC order lifecycle + wearable source rejection, SEO/JSON-LD). `npm run e2e` (prod build + standalone server + seeded Mongo on host :27019)
- [x] Link check across all routes (links.spec.ts crawls every internal href on every sitemap page)
- [x] Lighthouse ≥95 perf/SEO/a11y on Home + Pricing — Home 97/100/100, Pricing 97/100/100 (mobile default). Caption/footer grays darkened minimally + founder-note link underlined to pass WCAG 4.5:1 (smallest shifts that pass; noted as deliberate deviation from design token #7C887F → #5F6A62)
- [x] xcodebuild succeeds for iOS + watch targets — unblocked 2026-07-03 (`-runFirstLaunch` worked without sudo); BUILD SUCCEEDED locally, app installed + launched in iPhone 15 Pro simulator
- [x] `cdk synth` passes
- [x] `docker compose up --build` running and healthy, seeded — web :3000, mongo host :27019 (27017/27018 held by other projects), mongo-express :8083 (8081/8082 held by other projects)

## Security audit + hardening (2026-07-03) — COMPLETE

Multi-agent adversarial audit (8 attack surfaces, 3-lens verification) found 8 confirmed defects; all fixed + regression-tested + verified live. Details in docs/ASSUMPTIONS.md (security section) and the "Security hardening" commit.
- [x] CRITICAL: SESSION_SECRET/ADMIN_PASSWORD fail closed in production (src/lib/env.ts + instrumentation.ts) — no committed fallback secret in prod; admin cookie no longer forgeable
- [x] CRITICAL: GDPR Art.9 consent enforced server-side (src/lib/consent-guard.ts on all 8 health endpoints); withdrawal revokes sessions immediately
- [x] CRITICAL: real account erasure (POST /api/v1/account/delete → flag + revoke + E12 email + erasure_jobs; scripts/run-erasure.ts / `npm run erase:run`; retains consent audit trail)
- [x] CRITICAL: collision-safe runtime IDs (src/lib/ids.ts) + checkout upsert prevents duplicate/bricked memberships
- [x] HIGH: demo bearer token gated to non-prod / ALLOW_DEMO_TOKEN (web) and DEBUG-only (iOS)
- [x] HIGH: security headers globally (CSP frame-ancestors none, HSTS, X-Frame DENY, nosniff, referrer-policy; no-referrer + no-store on token/health pages) — verified live
- [x] HIGH: webhooks require shared secret in prod (STRIPE/LGC_WEBHOOK_SECRET), open only in dev/e2e
- [x] MEDIUM: bloodwork confirm array capped (max 100) + batched $in query
- [x] iOS: per-config base URL (Release=HTTPS) + ATS exception DEBUG-only; session token Keychain AfterFirstUnlockThisDeviceOnly; health values no longer persisted to UserDefaults
- [x] infra/CI: CDK bucket SSL/encryption/public-access-block verified; CI least-privilege `permissions: contents: read`
- [x] Verified: tsc clean, 156 vitest, 47 Playwright e2e, iOS Debug+Release BUILD SUCCEEDED, stack live + reseeded

## V2 — accounts, auth, commerce (handoff: design_handoff_v2/, started 2026-07-02 morning)

Spec: `design_handoff_v2/README.md` (rules) + `designs/AccountFlows.dc.html` (19 sections, all screens/emails/edge states). iOS Prototype.dc.html deferred until user hands over native design updates.

Non-negotiables: email + magic-link auth only (no social, no SIWA at launch); Eircode gate only at checkout for Essential/Performance (routing-key allowlist = config, fail → waitlist + Fusion cross-sell); GDPR Art.9 consent screen (3 purposes, research off by default, versioned, revocable); results never in email; dunning 0/3/10/14 → read-only pause, nothing deleted; renewal email cancel = equal weight; uploaded bloodwork → user confirms every AI value, self-reported = hollow gold dots; payment on web only (Stripe mock + Apple Pay on web mock).

### Phase 10 — v2 backend (src/lib + api + emails)
- [x] Models: Consent (purpose/version/timestamp/surface), WaitlistEntry (routing key, county), GiftCode, ReferralCode, ShareLink (expiry/revoked/access log), MagicLinkToken, Session; User gains optional passwordHash + failed-attempt cooloff; BiomarkerReading gains source: lab|self_reported
- [x] Member auth: session cookie, /join /signin magic-link + password flows, 30-min single-use links (60s resend throttle), 5-fail 15-min cooloff, non-revealing responses, reset signs out other sessions
- [x] Eircode eligibility: config collection seeded with launch allowlist (D01–D18, D20, D22, D24, D6W, A94, A96, K32, K34, K36, K45, K56, K67, K78), routing-key-only validation, rejected keys logged
- [x] Consent grants API (versioned, re-consent trigger), waitlist join/position, checkout API (mock Stripe), gift/redeem, GP share links, bloodwork upload confirm (mock AI extraction)
- [x] 11 transactional emails, one layout, rendered to Mongo outbox (E1–E11 per §12/§14)
- [x] Seed extensions + unit tests for eircode/magic-link/dunning logic

### Phase 11 — product web app (new routes)
- [x] /join /signin /verify /consent per §03–04 (edge states included)
- [x] /checkout (3 steps: eligibility → details → payment) /early-access /welcome per §05–07
- [x] /book /gift /redeem /s/[token] per §08, §15–16
- [x] /account /account/security /account/privacy per §10, §17 (delete flow: type-DELETE, export first)

### Phase 12 — marketing updates (v2 deltas only)
- [x] Pricing CTAs → /join (Fusion) and /checkout (Essential/Performance) with eligibility hint lines
- [x] "AI" copy replaces vendor names on Science + App pages (verify against v2 designs) — Science updated; App/Home designs are byte-identical v1→v2 (no changes needed); full-src grep clean of vendor names in marketing copy

### Phase 13 — admin additions
- [x] /admin/waitlist (demand by county), /admin/eligibility (Eircode config editor), /admin/consent (audit log) per §18

### Phase 14 — v2 verification
- [x] e2e: join→verify→consent flow (magic link via outbox), eircode pass (D08) / fail (T12) → waitlist, checkout mock, account pages, pricing CTA targets — 6 new specs / 16 tests (auth-flow, checkout, share, account, pricing-cta, admin-v2 + e2e/v2-helpers.ts)
- [x] Full regression: 40/40 Playwright e2e (24 v1 + 16 v2) green, 122/122 vitest green, tsc clean; docker stack rebuilt on the v2 image, seeded, /join + /s/k7f2demo verified live

## V3 — iOS + Watch rebuild (handoff: design_handoff_ios_watch/, started 2026-07-03)

Spec: `design_handoff_ios_watch/README.md` + `designs/Prototype.dc.html` (42 screens, 8 groups, `data-screen-label` sections; prototype logic class = state spec). Repo now on GitHub: origin = https://github.com/NiallJoeMaher/arcaevo.git — push after verified milestones.

Non-negotiables: email + magic-link auth only (universal links open the app); payments ALWAYS link out to web checkout, no IAP; GDPR consent screen (3 purposes, research off); results never in email/push — critical values = "Dr. Nolan would like a word first", never a red number; HealthKit read-only with primer-before-sheet; honest delete; wellness not diagnosis; AI narrates, rules decide. Watch: status + deltas only, never raw alarming values; hit targets ≥44px.

### Phase 15 — iOS foundation v3
- [x] Fonts: bundle Instrument Serif / Hanken Grotesk / Geist Mono (OFL) into targets, Theme v2 with prototype tokens; system-font fallback documented if bundling fails
- [x] ArcaevoKit v2 API client: magic-link request/verify, consents, eligibility, waitlist, uploads/bloodwork(+confirm), share, checkout link-out URLs; session-token auth alongside demo token
- [x] App state machine per prototype logic: plan (fusion/essential/performance), onboarding progress, eircode gate state, notification prefs (4 toggles, Face ID on), experiments state
- [x] Navigation shell: onboarding flow → free tier → member app tab bar (Today/Results/Experiments/Account)

### Phase 16 — iOS screens (36)
- [x] ONBOARDING (7): welcome, signup, verify, consent, healthkit primer+sheet, about-you, notifications
- [x] FREE TIER (2) + PURCHASE (4): free home, plans, eircode gate, waitlist, checkout link-out, success (plan-aware)
- [x] TESTING (4): activate kit, nurse booking, sample journey, critical value ("needs a word")
- [x] MEMBER APP (9): dashboard, fusion timeline (marker × signal pickers, scrubbable), results, marker detail (ApoB), insights, experiments, start experiment, verdict, ask Arcaevo chat
- [x] YOUR DATA (5): add bloodwork, confirm reading (low-confidence blocks), type by hand, timeline, GP share
- [x] ACCOUNT (6): hub, security, privacy, delete, invite, connected sources

### Phase 17 — Watch app (6)
- [x] complication/watch-face entry, today baseline, biomarker glance, quick-log, active experiment, result ready (pushes to phone) — in-app watch-face entry screen ships; the true WidgetKit accessory complication needs a widget-extension target in project.yml and is DEFERRED (see log)

### Phase 18 — native verification
- [x] xcodebuild local: -runFirstLaunch succeeded (no sudo needed), BUILD SUCCEEDED for Arcaevo scheme incl. embedded watch app (iPhone 15 Pro sim, iOS 17.2 runtime)
- [x] App running in simulator: installed + launched co.arcaevo.app on booted iPhone 15 Pro
- [x] Push to GitHub after green — pushed 2026-07-03

## Phase 19 — Mock email + personal seeds (2026-07-03)

Real mock-email infrastructure (MailHog over SMTP) + personal seed accounts. `nodemailer` + `@types/nodemailer` installed by the user for this phase.

- [x] docker-compose `mailhog` service (mailhog/mailhog:latest, container arcaevo-mailhog) — host **1026:1025** SMTP / **8026:8025** UI (1025/8025, 1027/8027, 1030/8030 are held by other local projects' mail catchers); web service env: `EMAIL_PROVIDER=mailhog`, `SMTP_HOST=mailhog`, `SMTP_PORT=1025`
- [x] `src/lib/vendors/email.smtp.ts` — nodemailer transport (SMTP_HOST default localhost, SMTP_PORT default 1026, no auth/TLS for MailHog, from `Arcaevo <hello@arcaevo.com>`). The Mongo `outbox` write stays ALWAYS (e2e + admin read it); with `EMAIL_PROVIDER=mailhog|smtp` the same email is ADDITIONALLY sent via SMTP, fire-and-forget with error logging — an SMTP failure can never break an API request. MOCKED_APIS.md §7 rewritten
- [x] `scripts/seed-user.ts` + `npm run seed:user` — personal sign-in-able account (args via env or `KEY=VALUE` argv: EMAIL required, NAME, PASSWORD default arcaevo-demo-2026, TIER default performance, WITH_DATA default 1): verified user w/ real random-salt scrypt hash, active membership, 3 consents granted (research ON — own account), 40 readings = 2 lab draws (computed baselines + RCV verdicts, "did it work?" story) + one 10-marker self-reported hollow-gold draw, 90d wearables, 3 orders incl. results_ready ×2 + shipped recheck, active GP share link (`/s/gp-<slug>`), referral code from the name. Idempotent per email (rerun refreshes only that user's docs); does NOT wipe other collections — but the main seed DOES wipe everything, so rerun seed:user after every `npm run seed`
- [x] E2E path: the seeded `demo@arcaevo.test` / demo-password-123 member (mem_0026) already covers the stable e2e password account — seed.ts unchanged
- [x] `e2e/email.spec.ts` — (a) signup → E1 lands in the Mongo outbox w/ a `/verify?token=` link; (b) the same email arrives in MailHog via SMTP w/ correct from/to/subject, `test.skip` with a clear message when localhost:8026 is unreachable (CI stays outbox-only). playwright.config.ts webServer env passes through EMAIL_PROVIDER/SMTP_HOST/SMTP_PORT with local defaults (`mailhog` / `localhost` / `1026`) and empty EMAIL_PROVIDER under CI
- [x] README quick-start updated (mailhog UI :8026, seed:user usage) + this section

### Log

- 2026-07-03: Phase 19 complete. VERIFIED: tsc clean · 122/122 vitest · **42/42 Playwright** (40 prior + 2 new email tests; the MailHog SMTP assertion ran for real, no skip) · docker stack rebuilt on the new image w/ mailhog wired, reseeded + seed:user rerun · `POST /api/v1/auth/signin` on the running stack → 200 `{member: mem_u_niall-codu-co, needsConsent: false}` for niall@codu.co / arcaevo-demo-2026 · a signup via the containerised web delivered "Confirm it's you" to MailHog over the compose network (7 messages total at http://localhost:8026). seed:user idempotency verified: double-run keeps 1 user / 1 membership / 40 readings / 360 wearables; the 27 other users untouched. Personal account seeded: mem_u_niall-codu-co · Niall Maher · performance €399/yr · referral NIALL-1T · share /s/gp-niall-codu-co.

## GDPR hardening (2026-07-03) — consent enforcement + real erasure

Two verified CRITICAL Art.9/Art.17 defects fixed on the consent/erasure surface.

- **FIX 1 — consent now enforced server-side** (was decorative). New `src/lib/consent-guard.ts` `requireConsentedMember(req, {clinicianReview?})` composes `requireMember` + a live `consentState()` check, mirrors the `{member}|{denied}` shape (one-line route adoption), 403 `{error:"consent_required", needsConsent:true}` when `health_processing` is absent/withdrawn or the account is suspended/closing. **Consent-guarded endpoints**: `POST /sync/wearables`, `GET`+`POST /orders` (POST also requires `clinician_review`), `GET /orders/[id]`, `GET /results`, `GET /insights`, `POST /uploads/bloodwork` + `/confirm`, `GET`+`POST /share`. Auth/`/consents`/waitlist/eligibility/public `/s/[token]` deliberately un-gated. Withdrawal = immediate stop: `POST /consents {granted:false}` (and the delete endpoint) call `suspendProcessingForWithdrawal` → flag user (`processingSuspended`/`status:"closing"`/`closureRequestedAt`) + delete every session doc (live cookies/bearers die → 401).
- **FIX 2 — "Delete everything" is now real.** New `POST /api/v1/account/delete` (requireMember): records the withdrawal, revokes sessions, flags closing, sends the new **E12** closure-confirmation email (+30d date, NO health values), enqueues an `erasure_jobs` doc `{eraseAfter:+30d, status:"scheduled"}`. New `scripts/run-erasure.ts` (`npm run erase:run`) + `src/lib/erasure.ts` hard-delete due members across ALL PII/health collections EXCEPT the `consents` audit trail (retained per DPC), idempotently. `ConsentSection.tsx` now calls the delete endpoint and only shows the confirmation after 200 (type-DELETE arming + export-first kept). MOCKED_APIS §16–17 document the prod cron requirement + the webhook-ingest coordination note.
- **Models/db**: `User` gains `processingSuspended?`/`closureRequestedAt?`/`status?` (optional — no seed churn); new `ErasureJob` model + `erasure_jobs` collection (added to the seed wipe). Seed already grants `health_processing` to Aoife (`mem_0001`) + `demo@arcaevo.test` (`mem_0026`) — verified, so existing e2e keep passing (no guard weakening).
- **Tests**: +2 vitest files (`consent-guard.test.ts` 8, `erasure.test.ts` 5) + E12 email test → **134/134 vitest green**; `tsc --noEmit` clean; `next build` clean (incl. `/api/v1/account/delete`). New e2e `e2e/consent-guard.spec.ts` (no-consent → 403 → consent → 200; withdrawal revokes session → 401) written but NOT run here (integrated Playwright pass is the main loop's, per coordinator — avoided :3000/Mongo collision).

## Wanted deps (agents append here instead of installing)

- `vitest` (dev, apps/web) — unit tests for lib logic: rcv.ts verdicts/baseline bands (pure functions, test-ready), stripe.mock refund rules, LGC mock state machine. Add `"test": "vitest run"` to scripts once installed.
- `@playwright/test` (dev, apps/web) — Phase 9 e2e suite (routes render, pricing verbatim, admin login + tabs, API smoke via mock LGC order lifecycle).

## Log

- 2026-07-03: Phase 16 (ONBOARDING 7 + FREE TIER 2 + PURCHASE 4 + TESTING 4 = 17 journey screens) complete — Views/OnboardingV3|FreeTierV3|PurchaseV3|TestingV3 + V3Shell.swift rewrite, pixel-faithful to Prototype.dc.html (light screens on the prototype's #ECE7DD phone canvas, dark on #1C2620), copy verbatim. ONBOARDING: WelcomeV3 (34px orb + 8px halo, serif-40 headline, Create account / Sign in — both magic-link), SignupV3 (email w/ green focus border, optional password field per design [captured only — APIClient has no password-signup endpoint; magic link drives the flow], tappable Terms checkbox, non-revealing 202 copy), VerifyV3 (✉ 64px roundel, bold email, resend; DEBUG-only paste-link/token affordance kept for the Mongo-outbox dev loop; offline → demo session), ConsentV3 (3 purpose cards: 2 REQUIRED fixed ✓, Anonymised research OPTIONAL—TAP TO CHANGE off-by-default; POST /consents surface:"ios"; Health Data Notice link → web /legal/privacy), HealthKitPrimerV3 (primer-BEFORE-sheet: bottom white card "Arcaevo would like to access…" whose Allow fires the REAL HKHealthStore request — MockHealthStore in sim; Not now skips), AboutYouV3 (DOB wheel-picker sheet defaulting 14/03/1991, Female/Male pills, 4 focus chips, persisted @AppStorage), NotificationsV3 (4 tappable rows, 40×23 toggles, Face ID ON default, weekly focus OFF; CTA fires the real UNUserNotificationCenter request then completeOnboarding). FREE TIER: FreeHomeV3 (dark; time-of-day greeting + first name; Apple Health summary + RHR sparkline cards driven by the REAL/mock wearable series [labelled "· 30 DAYS" honestly — AppModel loads 30d, design says 90; caption computed from the series]; exactly ONE 🔒 MEMBERS locked card; cream CTA "See plans — from €119/yr"), PlansV3 (€119/€329/€399 verbatim, Essential dark card + MOST POPULAR badge; fusion → checkout, essential/performance → gate; "payment happens on arcaevo.com" footer). PURCHASE: EircodeGateV3 (STEP 1 OF 3 eyebrow, free-typed mono field + the design's two tap-to-try codes; checkEligibility → D08 pass ✓ green → checkout, T12 fail → "Cork — not in the service area yet" #B3543A → waitlist; gate resets on entry like the prototype; routing-key-only footer), WaitlistV3 (joinWaitlist → "✓ You're № N for Cork" + founding-window note + Fusion cross-sell ghost → fusion checkout), CheckoutV3 (LINKS OUT ONLY: order-summary card verbatim, "Pay €… on arcaevo.com" → SFSafariViewController on checkoutURL(tier:); NO IAP, no card fields in-app; post-visit "I've finished checkout — continue" return affordance until the post-payment universal link exists; refund footer verbatim), SuccessV3 (plan-aware 01 step + CTA from the prototype plans map: fusion → activateMembership+Today [upload screen is DataV3's — no direct deep-link], essential → activate kit, performance → nurse booking; steps 02/03 verbatim; plan recorded onAppear). TESTING: ActivateKitV3 (dashed QR card = mock scan filling ARC-7F2K-D8 w/ success haptic, hand-typed mono code field works, CTA gated on code), NurseBookingV3 (Tue 7/Wed 8/Thu 9 × 07:30–09:45 morning grid, defaults Wed 8 08:15, re-pick resets booking, ✓ Booked card w/ fasting-reminder note, free-reschedule footer, Follow my sample journey → journey), SampleJourneyV3 (dark 5-stage timeline activated→posted→at lab→clinician review→results mapped from TestOrder.status [no order → design's at-the-lab state]; month eyebrow from order date; "never values in the email" verbatim; exit "Continue to your dashboard" → activateMembership; DEBUG-only demo trigger → critical value), CriticalValueV3 ("Dr. Nolan would like a word first." — calm, NEVER a red number or the value; call-window card, Choose a better time alert, "rest of your panel" → Results tab). Shell: V3Shell owns JourneyFlow NavigationStack (plans→gate→waitlist→checkout→success→testing as routes under .freeTier; membership activates on journey/critical/fusion exits), shared chrome (ArcPill/ArcGhostPill/ArcBackLink/ArcOrb/ArcToggle/V3SafariView/FlexibleWrap), haptics via .sensoryFeedback, all hit targets ≥44pt; ALSO wired the sibling agents' landed tabs per their notes (MemberTodayV3View/MemberResultsV3View/ExperimentsV3View/AccountV3View — placeholder structs kept compiling, unused). VERIFIED: whole-module swiftc -typecheck clean; xcodegen + xcodebuild Arcaevo scheme BUILD SUCCEEDED (own DerivedData /tmp/dd-journey); installed + launched on iPhone 15 Pro sim (17.2), welcome renders pixel-true. NOTE destination flake now reliably clears with: simctl shutdown all + launchctl remove com.apple.CoreSimulator.CoreSimulatorService + pkill -f CoreSimulator + sleep 12 + arch=arm64-qualified -destination.

- 2026-07-02: Loop started. Repo scaffolded, plan written.
- 2026-07-02: Phase 1 complete — design tokens mapped in globals.css @theme (colors, hairlines, radii, shadows; selection/link-hover/focus-visible base styles), fonts (Instrument Serif, Hanken Grotesk, Geist Mono via next/font) + base metadata (title template, metadataBase) in layout.tsx, SiteNav/SiteFooter components, pixel-faithful Home `/` and `/pricing` pages, default scaffold page + public SVGs removed. `npx next build` passes with zero errors.
- 2026-07-02: Phase 2 content extraction done — compare.ts (8 versus pages + compare index), articles.ts (4 blog posts + blog index), legal.ts (7 docs), help.ts (4 FAQ groups), index.ts barrel. Verbatim from designs; typed unions; `npx tsc --noEmit` passes.
- 2026-07-02: Phase 3 (part 1) done — six marketing pages built pixel-faithfully from designs: /how-it-works (4-step walkthrough, fusion-engine explainer + hs-CRP/HRV chart, "what lands in your app"), /science (4 pillars, RCV formula card + verdict pills, marker evidence, wellness-not-diagnosis safety bar → /legal/clinical-safety), /app (iPhone + Watch mockups, 6-feature grid, v1 = Apple Watch/Health/iPhone with WHOOP·Oura·Garmin "soon" dashed pill), /about (story, 3 values, stats band, team, careers/contact CTA), /careers (perks + 5 roles linking to /contact), /contact (channel list + client-component prototype form with sent-state, GP/112 disclaimer). Verbatim copy, per-route metadata, SiteNav active props, aria-hidden decorative visuals, labelled form fields. `npx next build` passes with zero errors.
- 2026-07-02: Phase 3 (part 2) done — data-driven pages built from src/content modules, pixel-faithful to designs: /compare (summary row + 8 competitor cards), /compare/[slug] (8 SSG versus pages: question H1, short-answer block, at-a-glance table, wins cards, honest take, People-also-ask, CTA + more-comparisons chips; FAQPage + BreadcrumbList JSON-LD, per-slug metadata), /blog (featured dark card w/ chart SVG + 3 gradient-glyph cards), /blog/[slug] (4 SSG articles: exhaustive ArticleBlock union render, answer-first, takeaways, CTA, related; Article + FAQPage JSON-LD), /legal/[doc] (7 SSG docs w/ sticky sidebar nav, rights card, verbatim prototype disclaimer footer) + /legal → /legal/privacy redirect, /help (category chips + 4-group accordion client component: one open across page, +/− swap, buttons w/ aria-expanded/aria-controls, item 0-0 open by default). One H1/page, semantic landmarks, breadcrumb navs. `npx next build` passes — all 19 static paths generate.
- 2026-07-02: Phases 5 + 8 complete — platform layer: src/lib (db.ts Mongo singleton + typed collection accessors, models.ts zod schemas incl. pricing/allowance constants, rcv.ts pure RCV verdict + baseline-band logic, auth.ts HMAC admin cookie + demo bearer token, analytics.ts PostHog-EU no-op stub) and src/lib/vendors (types.ts interfaces; letsgetchecked.mock.ts deterministic state machine + seeded results; stripe.mock.ts deterministic ids + refund rule; email.mock.ts → Mongo outbox — all loudly `// MOCK:` commented). 17 REST routes under /api/v1 (auth/demo, admin login/logout, members ×3, orders ×2 w/ tier-allowance + €99/€69/€199 add-on pricing, results + admin review queue/action, insights w/ deterministic templates + AI-narration slot noted, sync/wearables apple_health-only w/ roadmap rejection, LGC + Stripe webhooks (signature = documented no-op stubs), admin/kpis, admin/support). scripts/seed.ts (`npm run seed`, deterministic, anchor 2026-07-01): 25 members (demo mem_0001 Aoife Byrne ↔ demo-member-token), 15 biomarker rules, 13 orders across every pipeline state, 67 readings (30 in review queue) incl. full "did it work?" story (ApoB/LDL-C/glucose/hs-CRP improved beyond RCV at recheck), 360 wearable signals (90d × 4 types), 6 tickets, 4 outbox emails — verified against Mongo 7 in Docker. Dockerfile (deps→build→runner, node:20-alpine, standalone; next.config.ts output:"standalone"), .env.example. infra/cdk hand-written CDK v2 stack (eu-west-1: exports S3 bucket + 3 Secrets Manager placeholders; README: Vercel hosts web, Atlas external) — `npx cdk synth` passes; `npx tsc --noEmit` clean in apps/web. NOTE: host port 27017 was occupied by another project's container (assemblpro-mongodb) during this run — compose `mongo` couldn't bind; seed was verified against a temporary mongo:7 on :27019 (since removed). Free 27017 before the final `docker compose up --build`.
- 2026-07-02: Phase 7 built — apps/ios: XcodeGen project.yml (Arcaevo iOS 17 app w/ HealthKit entitlement + usage string, ArcaevoWatch watchOS 10 companion, shared ArcaevoKit sources group compiled into both targets); ArcaevoKit models (User/Membership/TestOrder/BiomarkerReading/WearableSignal/Insight) + async APIClient → http://localhost:3000/api/v1 w/ static demo bearer token + seeded DemoDataProvider fallback (app always demos); iOS screens (3-page onboarding + HealthKit prompt + mock sign-in, Today w/ readiness ring + Swift Charts sparklines + "did it work?" card, Results grouped by panel w/ baseline bands + RCV verdict tints, Orders w/ 6-step status timeline + add-on POST, Settings w/ tier + Apple Health state + export/delete links + disclaimer); HealthKit layer (real HKHealthStore HRV/RHR/sleep/VO2max behind HealthDataProviding, MockHealthStore auto-selected in simulator/denied); watch single-view today ring + latest insight + test status; apps/ios/README.md. `xcodegen generate` OK; xcodebuild blocked locally (Xcode system content missing, `-runFirstLaunch` needs admin) — verified instead via full `xcrun swiftc -emit-object` compile of both targets against iOS + watchOS simulator SDKs (clean).
- 2026-07-02: Phase 9 unit tests done — vitest suite for apps/web lib: vitest.config.ts (node env, src/**/*.test.ts, @/* alias) + `test`/`test:watch` scripts; 5 files / 69 tests under src/lib/__tests__ (rcv.ts verdict math incl. inclusive RCV boundary + zero-prior guard + baseline bands/rounding/negative means; models.ts seed-shaped docs accepted, bad enums rejected (tier/order status/wearable source), pricing constants verbatim 119/329/399 + 130 + 99/69/199 and tier allowances; stripe.mock refund rule (full refund only while "ordered", 0 after) + deterministic cs_mock_/sub_mock_ ids; letsgetchecked.mock state machine (ordered→…→results_ready one step per poll, forward-only clamp) + seeded result determinism via an in-memory Mongo fake that evaluates the vendor's real $set/$min/$add pipeline — no Mongo spun up; auth.ts HMAC sign→verify roundtrip, tampered payload/sig, non-admin role signature, wrong SESSION_SECRET, ADMIN_PASSWORD checks via vi.stubEnv). next/headers + @/lib/db stubbed in unit tests; cookie-store integration + real-Mongo LGC/pipeline behavior deferred to Playwright e2e. No lib bugs found. `npm test` all green; `npx tsc --noEmit` clean.

- 2026-07-02: Phase 12 complete — v2 marketing deltas applied. Pricing plan CTAs rerouted: Start Fusion /contact → /join (hint "Available everywhere — nothing ships"), Start Essential /contact → /checkout (hint "Dublin service area — quick Eircode check first"), Start Performance /contact → /checkout (same hint); hint lines styled per Pricing.dc.html (11.5px centered, caption / muted-dark-soft). Science copy de-vendored per v2 design: hero "Claude rewrites" → "The AI rewrites", pillar 04 "Claude turns the rule output" → "The AI turns the rule output". v1→v2 design diff confirmed only Pricing + Science changed (App/Home/Versus/Article byte-identical); src grep shows no vendor names left in marketing copy (remaining "Claude" is a code comment in api/v1/insights, out of marketing scope). `npx next build` green; verified via curl of fresh prod build on :3100 (all pricing figures verbatim, new hrefs + hints present, no vendor names on /, /app, /science). NOTE: e2e webServer port :3000 is held by the docker compose web container (stale pre-v2 image), so pricing.spec.ts wasn't run against the new build — spec only asserts figures, all curl-verified verbatim; rerun e2e after docker image rebuild or with :3000 free.

- 2026-07-02 (04:40): Phase 9 complete (bar blocked xcodebuild). E2E: 24 Playwright tests green (routes/links/pricing/help/admin/API/SEO). Lighthouse Home + Pricing 97/100/100. A11y contrast fixes: caption #7C887F→#5F6A62 (globals token), strip label #9AA39C→#6C756E, footer #6E7E74→#7E8E84 + #5B6A61→#79897F, founder-note link underlined. Full docker stack up + seeded (web :3000, mongo :27019, mongo-express :8083). vitest 69 green, tsc clean. BUILD DONE.

- 2026-07-02: Phase 13 complete — the three §18 admin ops views. /admin/waitlist (ADM-1 "Where do we open next?"): 4 KPI cards (total signups / counties / top county / oldest entry), dark demand card w/ per-county bars + 1,000-signup-line note, county table (signups, top routing keys, oldest entry). /admin/eligibility (ADM-2 "The gate is data, not a deploy"): live allowlist as removable chips + add form (AllowlistEditor client component), Cork STAGED card (design mock — no staging in config schema), change-history card, rejected-key log table (key only) + 7-day rejection KPIs. /admin/consent (ADM-3 "Prove it, per person, per purpose"): current-notice-version banner (amber + members-needing-re-consent count when versions mismatch, green when all current), purpose filter chips via ?purpose= searchParams, audit table (member, purpose, GRANTED/DECLINED pill, version w/ amber RE-CONSENT DUE on stale latest grants, surface, timestamp). Loaders added to (panel)/data.ts w/ same withTimeout/null-on-Mongo-down pattern; changeLog typed there as EligibilityConfigDoc (models.ts untouched — changeLog lives outside the zod schema). New API: GET/POST /api/v1/admin/eligibility (requireAdmin; POST {add?,remove?} normalises via extractRoutingKey, 400s invalid keys, appends {at,added,removed} to config changeLog, upserts). Sidebar gains Waitlist/Eligibility/Consent audit items. Verified live on seeded :27019 (login → GET config 31 keys → POST add d25/T12+remove D01 → changeLog written → pages render real data → filter works → 401 unauthenticated → config restored to seed). tsc clean for Phase-13 files; `npx next build` green in an isolated HEAD+Phase-13 worktree (repo-wide build currently blocked by the in-flight Phase 11 agent's half-written /checkout route, not by this work).
- 2026-07-02: Phase 10 complete — v2 backend. src/lib: models.ts v2 section (Consent/WaitlistEntry/GiftCode/ReferralCode/ShareLink/MagicLinkToken/Session/EligibilityConfig/EligibilityRejection/BloodworkUpload + CONSENT_VERSION; User gains passwordHash/emailVerified/failedAttempts/cooloffUntil; Membership gains "pending" status + dunningStage/dunningStartedAt; BiomarkerReading gains source lab|self_reported — seed migrated), db.ts 10 new typed collections, member-auth.ts (opaque 256-bit session tokens stored SHA-256-hashed in `sessions` — chosen over signed cookies for per-session revocation; scrypt passwords; 30-min single-use magic links w/ 60s throttle, hash-only storage; 5-fail→15-min cooloff; old demo bearer still works — auth.ts memberFromRequest now resolves demo bearer OR bearer session token OR session cookie), eligibility.ts (extractRoutingKey case/space tolerant incl. D6W, allowlist from `eligibility_config` seeded w/ 31 launch keys, rejects logged key-only), dunning.ts (pure 0/3/10/14 ladder, paused=read-only, resolve=instant resume), consents.ts (append-only versioned grants + re-consent detection), emails.ts (E1–E11, one layout, verbatim §12/§14 copy; E7 params make values unrepresentable; E8 equal-weight cancel; sent via outbox), vendors/ai-extraction.mock.ts (deterministic per-value-confidence extraction, "41 or 47?" flag blocks confirm). 18 new/extended routes: auth/signup·magic-link(+verify)·signin·signout·reset(+confirm), consents GET/POST, eligibility/check, waitlist POST/GET, checkout (guest-inline, server-side gate, pending membership), gift + gift/redeem (year starts at activation, buyer gets one data-free email), share GET/POST + share/[token] GET public w/ access log/DELETE revoke, uploads/bloodwork + confirm (writes self_reported readings), webhooks/stripe extended (dunning progression, E9 on first fail only, E4 on checkout completion, invoice.paid resumes). Seed: eligibility config, 3 rejections, 2 waitlist entries, 6 consents, share link /s/k7f2demo, GIFT-DEMO-2026, AOIFE-K4, self-reported vitamin_d point, e2e password member demo@arcaevo.test / demo-password-123 (mem_0026, fixed-salt scrypt, no membership). Tests: +53 vitest (eligibility 13, member-auth 15, dunning 8, emails 14 + 3 updated model fixtures) → 122 green; tsc clean; next build passes; seed verified on :27019; all new routes smoke-tested live (signin/cooloff msg, magic-link verify single-use, throttle 429, checkout D08 pass/T12 403, dunning day0→day3 via webhook, upload→confirm, share revoke→410, outbox E1/E2/E4/E9 verified). MOCKED_APIS.md §11–15 added (AI extraction, member auth notes, Apple Pay via Stripe mock, GeoIP, clinician persona).

- 2026-07-02: Phase 14 complete — v2 e2e + full regression. 6 new specs / 16 tests under apps/web/e2e (+ v2-helpers.ts: Mongo outbox token fishing, demo signin UI/API helpers): auth-flow (join → E1 outbox magic link → /verify → /consent research-off → free /account; wrong-password verbatim 401 copy + magic-link promotion; signed-out /account → /signin), checkout (UI password signin → D08 blur pass state → details w/ DOB → mock pay → /welcome "kit ships today" → /account ACTIVE "0 of 2 tests used"; T12 → "Not in Cork yet" carrying eircode → /early-access → Cork position 2), share (/s/k7f2demo IMC 412887 + verdict rows; unknown token = designed gone state on HTTP 200; API open ×2 grows accessLog exactly +2), account (research toggle POST + persist + restore; GP link create → listed → revoke → gone; sessions list w/ NOW; type-DELETE arming only — never submitted), pricing-cta (Fusion→/join, Essential+Performance→/checkout, both hint lines verbatim), admin-v2 (waitlist Cork, 31 allowlist chips incl. D08 + editor present, consent audit + ?purpose=research filter). REAL BUG found & fixed in CheckoutClient.tsx: React reused the step form/button DOM across step transitions, so a click on the eligible-path "Continue to your details" (type=button) had its default action land on step 2's submit button after the synchronous re-render — form submitted with empty DOB (the `dob && !parseDob(dob)` guard passes on empty) → users skipped the details step straight to payment. Fix: unique key per step form (fresh mount, no node reuse). Also learned: interpolated JSX headings ("Not in {county} yet…") produce accnames without inter-node spaces ("Not in Corkyet") — specs match /Not in Cork/. Full suite: 40/40 e2e (9.6s, workers 1, reseeded), 122/122 vitest, tsc clean. Docker stack rebuilt on the v2 image (`docker compose up --build -d`), reseeded on :27019, /join 200 + /s/k7f2demo serving live. NOTE: Start Performance links to bare /checkout (no ?tier=performance) so it opens the essential flow — matches the task-specified assertion, flagged for a follow-up decision.
- 2026-07-02: Phase 11 complete — product web app, 14 new routes pixel-faithful to AccountFlows §03–§17, all off SiteNav (minimal orb+wordmark header per the mocks; shared pieces in src/components/account: ui.tsx tokens/AuthShell/Card, session.ts cookie→member for server components, InboxCard W2, SignOutButton). /join (W1 + §03 edge states: non-revealing already-registered, optional password w/ min-10 inline error, 60s resend throttle surfaced, terms checkbox → /legal), /signin (W3: verbatim wrong-password + cooloff copy, magic-link first-class button promoted to primary after a failure, Forgot? → reset), /verify (token redeem → needsConsent ? /consent : /account; &reset=1 → new password → reset/confirm signs out other sessions; expired/used → "only live 30 minutes" + send-a-fresh-one w/ ?email= prefill; tokenless W2), /consent (W4 verbatim, 2 required fixed on, research OFF, POST /api/v1/consents, ?next=checkout honoured), /checkout (W5→W7→W8 one route: eircode gate w/ blur-check pass state, fail → inline W6 linking /early-access?eircode= + Fusion cross-sell; details incl. guest email + DOB dd/mm/yyyy→ISO; mock card + Apple Pay both POST /checkout then fire checkout.session.completed webhook → /welcome, name/email stashed in sessionStorage for guests), /early-access (W6/X5: county lookup from carried eircode, position+county receipt, already-eligible 409 → checkout, Fusion card — never a dead end), /welcome (W9 plan-aware: essential kit steps verbatim, performance 01→Book your nurse → /book, fusion upload-first), /book (A2: deterministic next Tue/Wed/Thu × 4 morning slots, fasted + free-reschedule notes; TODO documented — no booking API yet, local confirm), /gift + /redeem (R2 + §16: delivery choice, note, POST /gift → code + mock checkout id; redeem member-authed w/ outside-Dublin choice → real waitlist join + Fusion checkout or /contact refund), /s/[token] (G2: server-side fetch of /api/v1/share/[token] force-dynamic so every open logs, reviewer + IMC, prev/current/verdict rows, Download PDF/Print via print stylesheet, designed gone-state for revoked/expired/unknown), /account (W10: sidebar 6 items/3 live, dark membership card w/ status chips, dunning X2 banners D0 + paused w/ Update card → invoice.paid instant resume, Cancel renewal one-confirmation → subscription.deleted, waitlist position card, quarterly +€130/yr [locked price; §10 mock's €140 superseded], export row TODO no API), /account/security (W12: passkey + TOTP as designed COMING states — not faked, password change via reset-link flow, real sessions list w/ End session / Sign out all server actions on hash-only rows), /account/privacy (W11: 3 consent switches wired to POST /consents, health_processing off → converged type-DELETE closure flow w/ export-first step; GP share links list create/revoke wired w/ "Opened twice — Dublin, 3 July" access log). tsc clean, next build green. Verified live (standalone :3100, seeded :27019): join → outbox E1 token → verify (re-use → link_expired verbatim) → consent → checkout D08 essential → webhook → /welcome "You're a member" + /account ACTIVE 0-of-2; T12 → Cork waitlist pos 2 + E10; /s/k7f2demo renders reviewer/rows/verdicts, revoked → gone state; gift → GIFT code → redeem T12 403 choice / D08 activates + buyer email; dunning webhook → PAST DUE banner → invoice.paid → ACTIVE; signed-out /account|/consent|/book 307 → /signin. Deferred: booking/export/erasure/card-portal backends (documented TODOs in-place), gone-state pages return HTTP 200 (screen matches design; API itself 410s).
- 2026-07-03: Phase 15 complete — iOS foundation v3. FONTS: 10 static TTFs downloaded via google-webfonts-helper (fonts.gstatic, latin subsets) into apps/ios/Fonts — Instrument Serif 400+italic, Hanken Grotesk 300/400/500/600/700/800, Geist Mono 400/500 — plus per-family OFL.txt; bundled as resources into BOTH targets (project.yml sources buildPhase:resources) with UIAppFonts in both Info.plists; PostScript names verified in the TTF name tables (HankenGrotesk-SemiBold etc.). ArcaevoKit/Typography.swift: Font.arcSerif(size,italic:)/arcSans(size,weight:)/arcMono(size,weight:) with cached UIFont availability checks → graceful system serif/sans/mono fallback per face (surfaced in Account placeholder if any family fails to register). Theme.swift gains the v3 tokens (arcDarkSurface #1C2620, arcCream #F4F1EA, arcPrimaryGreen #34A07C, arcDeepGreen #1E5C45, arcBrightGreen #7FD3AE, arcMutedOnDark #8FA89A, arcSecondaryLight/Dark #7C887F/#4A554D, arcHollowGold #D9C9A4, rail tones, tab-bar #141B17). API v2 (ArcaevoKit — ModelsV2.swift shapes mirror the zod contracts): requestMagicLink(email,purpose) 202/429, verifyMagicLink(token)→Session{sessionToken,needsConsent,member}, get/postConsents(grants,surface:"ios") (3 purposes, research off), checkEligibility(eircode), joinWaitlist(email,eircode)/waitlistPosition, uploadBloodwork(kind,fileName|manualValues)→extraction w/ flagged low-confidence, confirmBloodwork(uploadId,values,takenAt), shareLinks/createShareLink/revokeShareLink, checkoutURL(tier) = webBase/checkout?tier=… (link-out only, NO IAP); structured APIError.server(status,code,message) surfaces backend copy verbatim. Auth ladder: keychain session token (KeychainHelper + SessionStore, generic-password co.arcaevo.app) → legacy demo-member-token fallback; DemoDataV2.swift stands in for every new endpoint offline (Dublin allowlist, Cork→waitlist pos 2, the "41 or 47?" flagged ferritin extraction, k7f2demo share link). STATE (Arcaevo/AppState.swift, @Observable, UserDefaults-persisted arcaevo.appState.v3): AppPhase onboarding(welcome→signup→verify→consent→healthkit→aboutYou→notifications)→freeTier|member(tier); EircodeGateState unchecked/pass/fail; NotificationPrefs 4 toggles w/ Face ID ON default; ActiveExperiment what/duration/watchedMarker/daysLogged/verdict; UploadConfirmState w/ isBlocked while low-confidence unresolved; relaunch resumes mid-flow. SHELL (Views/V3Shell.swift + RootView v3): skeletal-but-wired onboarding steps (signup posts magic link; verify screen has DEBUG-only paste-link/token affordance since dev emails land in the Mongo outbox; consent posts surface:"ios"; healthkit primer-before-sheet), FreeHomeView plans+gate+waitlist w/ web checkout link-out, MemberShellView custom ArcTabBar per prototype (bar #141B17, glyphs ☉▤⟳◎, active #7FD3AE 700 / inactive #5E6E64 500, ≥44px targets) wrapping existing Today/Results + Experiments/Account placeholders (legacy MainTabView/OnboardingView kept compiling). LINKS: onOpenURL handles https://…/verify?token= and arcaevo://verify?token= → verifyMagicLink; arcaevo scheme registered (CFBundleURLTypes), associated-domains entitlement documented as a commented block in project.yml (needs real team + AASA). VERIFIED: xcodegen generate + BUILD SUCCEEDED — Arcaevo scheme (iPhone 15 Pro sim; NOTE Xcode 16.4: must pass OS=17.4/17.2 explicitly since the 18.5 runtime isn't installed, and destination resolution intermittently fails until `launchctl remove com.apple.CoreSimulator.CoreSimulatorService` restarts the sim service) and ArcaevoWatch built both embedded AND directly via `-target ArcaevoWatch -sdk watchsimulator` (no watchOS sim runtime installed, so no concrete watch destination exists). App installed + launched on the 17.4 sim: welcome renders all three families (serif display/mono eyebrow/sans body), fonts + OFL files present in both .app bundles, arcaevo:// prompt confirmed.
- 2026-07-03: Phase 16 (YOUR DATA + ACCOUNT) + Phase 17 (Watch) complete — pixel-faithful to Prototype.dc.html, verbatim copy. YOUR DATA (Views/DataV3/, 5 screens + DataV3Support shared chrome): AddBloodworkV3View (photo → PhotosPicker, PDF → fileImporter(.pdf), manual → TypeValues; photo/PDF run appState.beginUpload mock-AI extraction then push confirm; "Processed in the EU" footer), ConfirmReadingV3View (every extracted value shown w/ units; low-confidence amber card renders the "was this 41 or 47?" chooser generically from `alternatives` with the prototype's "4_" shared-prefix placeholder; confirm CTA swaps to a BLOCKED grey pill "Resolve ferritin to continue" while `UploadConfirmState.isBlocked`; confirm → confirmBloodwork → self_reported readings → timeline), TypeValuesV3View (marker search over a 6-marker hand-entry catalog, value + unit chips per marker, Date-of-draw REQUIRED w/ inline graphical picker, add → ✓ entry cards w/ Edit + "+ Add another marker" + "Save — see it on your timeline" via uploadBloodwork(kind:.manual)+confirm), DataTimelineV3View (dark; event-driven chart — dashed muted segments between self-reported points, solid green into lab points; HOLLOW GOLD dots = self-reported vs solid green = lab; legend; "WHAT THIS UNLOCKS" card; cream "Share with your GP" CTA), GPShareV3View (loads shareLinks() [demo: k7f2demo], active-link green card w/ "Active · expires 1 Aug · opened N times · Revoke" + access-log line "Opened twice — Dublin, 2 July", create 30-day link via createShareLink, revoke via DELETE; Done dismisses). ACCOUNT (Views/AccountV3/, 6 screens + NotificationPrefsV3View row target): AccountV3View (EXACT name for shell wiring; owns its NavigationStack; api.me() w/ demo fallback; dark plan card "Essential · €329/yr · ACTIVE / Renews … · 1 of 2 tests used · billing on arcaevo.com"; rows → security/privacy/connections/invite/notifications + static Help & support; Brand.disclaimer footer), SecurityV3View (email magic link = PRIMARY card; passkey + TOTP rendered as designed COMING states — NOT faked; password Change links out to web account security; sessions card w/ NOW + End session → "Ended ✓"), PrivacyV3View (3 consent rows: required ones are static-toggle doorways — health_processing row navigates into the delete flow; research toggle live via postConsents(surface:"ios") + getConsents on load; "Export my data" request state [TODO: export API doesn't exist yet, same gap as web]; GP-share-links row → GPShareV3View; red-bordered delete entry), DeleteAccountV3View (honest: export offered FIRST, REAL type-DELETE TextField arms the button (opacity 0.4 until armed), delete = postConsents(health_processing:false) → backend closureRequired; done state shows computed erasure date (+30d) + pro-rata refund copy; prototype's "Restart the prototype" chrome replaced by Sign out → welcome), InviteV3View (NAME-XX code — demo member maps to seeded AOIFE-K4, others get stable hash suffix; tap-to-copy card + real ShareLink share sheet; give-a-month/get-a-month copy verbatim; "No leaderboards, no streaks" footer), ConnectedSourcesV3View (Apple Health row bound to AppModel.healthAuthorized REAL permission state w/ working Connect; Oura/WHOOP/Garmin rows verbatim subs w/ ON THE ROADMAP tags — never fake-connect per v1 rule). WATCH (ArcaevoWatch/ fully rewritten: ArcaevoWatchApp + WatchModel + WatchRootView + WatchScreens; v1 WatchTodayView.swift deleted): 6 screens as a .verticalPage TabView in prototype swipe order — face entry (live H:mm clock via TimelineView, baseline mini-ring → Today, T−12 NEXT TEST gold), today-baseline (92pt ring + "Steady"/"Within your baseline band. Nothing needs you today." derived from the deterministic Readiness score), biomarker glance (INFLAMMATION · HRV PROXY, latest HRV + gold sparkline from the demo series, verbatim calm caption — status+deltas only, never raw alarming values), quick-log (6 tags, 2-col grid, ≥44pt cells, wlogged state w/ WKInterfaceDevice .click haptic, caption swaps to "N tagged today → feeds your experiments"), active experiment (EVENING WALKS Day 51 of 60, progress bar, adherence 87%, "Log today's walk" → .success haptic → "✓ Logged — 52 of 60"), result ready (green dot ARCAEVO · NOW, "Results in.", no-red-numbers copy, cream "Read on iPhone" — pushes the member to the phone). Bundled fonts (arcSerif/arcSans/arcMono) used throughout both platforms; all hit targets ≥44pt. COMPLICATION DEFERRED: a real watch-face complication needs a WidgetKit accessory widget-extension target (new project.yml target + scheme wiring — outside this wave's scope); the face-entry screen carries the design's content in-app meanwhile. #if DEBUG preview harnesses: DataV3PreviewHarness + AccountV3PreviewHarness + per-screen #Previews (watch screens too). VERIFIED: xcodegen generate + xcodebuild Arcaevo scheme (iPhone 15 Pro sim, OS=17.4, own DerivedData /tmp/dd-data) BUILD SUCCEEDED first pass — both Arcaevo.app and embedded ArcaevoWatch.app binaries produced. Not wired by design: shell owner connects AccountV3View as the Account tab and routes into AddBloodworkV3View/DataTimelineV3View from Member screens.

- 2026-07-03: Phase 16 MEMBER APP (9) complete — apps/ios/Arcaevo/Views/MemberV3/ (this agent's dir only; V3Shell untouched). Screens pixel-faithful to Prototype.dc.html member sections, copy verbatim, all on #1C2620: MemberTodayV3View (health-score ring 74-style w/ live readiness score, "Up 3 since June.", JULY PANEL·REVIEWED → Results tab, FOCUS THIS WEEK → InsightsV3View, TODAY·APPLE WATCH stats from live wearable series, FUSION·APOB×RESTING HR mini-sparkline → FusionTimelineV3View, ACTIVE EXPERIMENT card → Experiments tab; ✳ Ask entry in header since chat isn't a tab in the 4-tab v3 bar — flagged deviation); FusionTimelineV3View (marker chips APOB/FERRITIN/HS-CRP × gold wearable chips RESTING HR/HRV/SLEEP, Swift Charts in the prototype's 300×92 design space: baseline band, continuous gold Watch line, blood draws lab=solid green / self-reported=hollow gold ALWAYS, tap-to-select ≥44pt points + DragGesture scrubber w/ vertical rule + month/value bubble (y→real-unit mapping 61→54bpm / 38→52ms / 6h40→7h15) + snap-to-draw haptics, per-draw note card + wear same-period card, verbatim footers); MemberResultsV3View (34 IN RANGE/3 WATCH/1 ACT chips, 5 system groups w/ amber watch tints — never red, Cardiovascular → MarkerDetailV3View, "Compared with February" footer); MarkerDetailV3View (ApoB 0.94 ↓ from 1.12, ABOVE OPTIMAL—MOVING THE RIGHT WAY chip, history chart w/ OPTIMAL<1.00 band + hollow-gold FEB25 self-reported draw, WHAT IT IS / WHAT MOVED IT·BLOOD+WATCH verbatim, cream CTA → Experiments tab); InsightsV3View (top-3 cards 01 BLOOD+WATCH/02 BLOOD/03 WATCH verbatim + "AI writes the words" footer); ExperimentsV3View (active card — AppState experiment if running (adherence = Watch-read demo ~87% of elapsed days ∨ quick-logs) else the design's Evening-walks 87% card, completed caffeine row → VerdictV3View, concluded AppState experiments also listed w/ IT WORKED/WITHIN NOISE/WRONG WAY(amber) chips, 2 data-suggested rows PICK→✓ QUEUED, Start something else → StartExperimentV3View); StartExperimentV3View (what/duration/watched chips per prototype expSel incl. defaults 4 weeks/Ferritin, disabled-until-picked green CTA "Start — X, Y" → AppState.startExperiment + ✓ Running card, .sensoryFeedback on picks + .success on start); VerdictV3View ("It worked." + deltas + THE HONEST PART verbatim; parameterised Model.from(ActiveExperiment) for real verdicts — within-noise says "Within the noise." honestly, worsened is amber never red); AskArcaevoV3View (empty state + 4 designed prompt chips + free-text composer; replies from Mv3Narrator — `// MOCK: AI chat`, deterministic keyword-routed narration grounded in the seeded July-panel story (ApoB → 1.21→1.12→0.94 walks narrative), flagged values → amber-bordered "with Dr. Nolan" handoff, honest fallback, "Narrates your numbers — never diagnoses" footer). Shared Mv3* chrome + MemberV3Demo story data (fusionDB/wearDB/chatQA verbatim from the prototype logic class) namespaced to avoid concurrent-agent collisions; MemberV3PreviewHarness (#if DEBUG) keeps all 9 reachable pre-wiring. NOTE: BiomarkerReading has no `source` field (contrary to brief) — lab/self-reported lives on Mv3FusionPoint locally; hoist to ArcaevoKit when DataV3 lands it. VERIFIED: swiftc -parse clean; whole-module swiftc -typecheck (iphonesimulator SDK, arm64-ios17-sim, -D DEBUG) zero errors; xcodebuild -target Arcaevo (own SYMROOT /tmp/dd-member) BUILD SUCCEEDED incl. embedded watch app. Scheme+sim-destination builds intermittently failed destination resolution ("iOS 18.5 not installed" placeholder) — CoreSimulatorService contention w/ concurrent agents, not code; first scheme run compiled everything and errored only on another agent's then-in-flight AccountV3 file (since landed). Shell owner: wire MemberTodayV3View/MemberResultsV3View/ExperimentsV3View as tabs (each owns its NavigationStack — don't double-wrap); Fusion/Insights/Ask ride on Today.
